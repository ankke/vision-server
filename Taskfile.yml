# https://taskfile.dev

version: '3'

tasks:
  format:
    cmds:
      - black .
  linter:
    cmds:
      - flake8 .
  run_dev:
    cmds:
      - FLASK_ENV=development flask run
  setup_db:
    cmds:
      - alembic upgrade head
